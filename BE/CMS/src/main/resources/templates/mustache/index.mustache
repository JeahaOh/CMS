{{>mustache/layout/header}}
<h1>title</h1>
<div class="col-md-12">
  
  <div class="row">
    <div class="col-md-6">
      <a href="/posts/save" role="button"
        class="btn btn-primary">regist post</a>
    </div>

    <div class="col-md-6">
      {{#userName}}
        Logged in as : <span id="user">{{userName}}</span>
        <a href="/logout" role="button"
          class="btn btn-info active">Logout</a>
      {{/userName}}
      {{^userName}}
        <a href="/oauth2/authorization/google" role="button"
          class="btn btn-success active">Google Login</a>
      {{/userName}}
    </div>
  </div>

  <table class="table table-horizontal table-bordered">
    <thead class="thead-strong">
      <tr>
        <th>no</th>
        <th>title</th>
        <th>author</th>
        <th>last modi date</th>
      </tr>
    </thead>
    <tbody id="tbody">
      {{#posts}}
        <tr>
          <td>{{id}}</td>
          <td>
            <a href="/posts/update/{{id}}">
              {{title}}
            </a>
          </td>
          <td>{{author}}</td>
          <td>{{modifiedDate}}</td>
        </tr>
      {{/posts}}
    </tbody>
  </table>
</div>
{{>mustache/layout/footer}}